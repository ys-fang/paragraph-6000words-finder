# 英文文章單字比對工具 - Cursor AI 專案指南

## 📋 專案概述

這是一個用於分析英文文章並與高中 6000 單字資料表進行比對的工具專案。專案提供兩個主要版本：

> ⭐ **推薦預設版本**: `index_nlp.html` (NLP 版本)
> - 這是專案的**推薦和預設版本**
> - 開發新功能時應優先參考 NLP 版本
> - 包含所有最新功能和改進
> - 提供更準確的詞形還原和更豐富的功能

- **⭐ NLP 版本** (`index_nlp.html`) **[推薦預設]**: 使用 Compromise.js 進行智能詞形還原，預期更高準確率（95%+），包含人工 QA、選擇性下載等進階功能
- **輕量版本** (`index.html`): 無外部依賴，完全離線運行，使用自定義詞形還原規則（91.7% 準確率）

## 🏗️ 專案架構

### 核心組件

1. **資料載入器** (`loadVocabularyData()`)
   - 從 `senior6000words.tsv` 載入單字資料
   - 處理多種單字格式：
     - 斜線分割：`a/an` → `["a", "an"]`
     - 括號變化：`enjoy(ment)` → `["enjoy", "enjoyment"]`
     - 組合格式：`actor/actress` → `["actor", "actress"]`
   - 解析 TSV 格式，提取欄位：Label, 英文, 音標, 中譯, CEFR 等級等

2. **詞形還原引擎**
   - **輕量版本**: `getLemma(word)` - 自定義規則
     - 不規則動詞對照表（40+ 常用動詞）
     - 動詞變化：-ing, -ed, -ied
     - 名詞複數：-s, -es, -ies
     - 形容詞比較級/最高級：-er, -est, -ier, -iest
     - 副詞：-ly
     - 形容詞後綴：-ful
   - **NLP 版本**: `getLemmaWithNLP(word)` - Compromise.js
     - 動詞不定式轉換
     - 名詞單數化
     - 形容詞基本形式識別
     - 基於上下文的詞性識別

3. **文章分析器** (`processArticle()`)
   - 提取文章中的所有英文單字
   - 去除重複單字
   - 對每個單字進行詞形還原
   - 在 6000 單字表中查找匹配

4. **結果展示系統**
   - 統計資料顯示（總單字數、找到的單字、未找到的單字、涵蓋率）
   - 表格展示（序號、文章中的單字、單字原型、對應標籤、中譯、CEFR 等級）
   - 篩選功能（全部/找到的/未找到的）
   - TSV 下載功能

5. **NLP 版本額外功能**
   - 人工 QA：可編輯單字原型
   - 重新檢索：修正後重新比對 6000 單字
   - 選擇性下載：勾選想要的單字再下載
   - 快速選擇：按等級（L1-L6）批次選擇
   - 即時統計：各等級選擇數量

## 📁 檔案結構

```
paragraph-6000words-finder/
├── index.html              # 輕量版本網頁應用程式（無外部依賴）
├── index_nlp.html         # NLP 版本網頁應用程式（使用 Compromise.js）
├── test.js                # 命令列測試腳本（Node.js）
├── senior6000words.tsv    # 6000 單字資料表（TSV 格式）
├── test_para.txt          # 測試用英文文章
├── README.md              # 專案說明文件（英文）
├── 使用說明.txt           # 快速使用指南（繁體中文）
├── 版本比較.md            # 兩個版本的詳細比較
├── DEPLOYMENT.md          # 部署資訊
└── .cursorrules           # Cursor AI 專案指南（本檔案）
```

## 🔄 工作流程

### 輕量版本工作流程 (`index.html`)

1. **頁面載入**
   - 自動載入 `senior6000words.tsv` 資料
   - 初始化 UI 元件

2. **使用者輸入**
   - 使用者在 textarea 中貼上英文文章
   - 點擊「🔍 開始分析」按鈕

3. **文章處理**
   - 使用正則表達式提取所有英文單字：`/\b[a-zA-Z]+\b/g`
   - 去除重複單字（使用 Set）
   - 對每個單字執行 `getLemma()` 進行詞形還原
   - 在 `vocabularyData` 中查找匹配（先查原型，再查原始單字）

4. **結果顯示**
   - 計算統計資料（總數、找到、未找到、涵蓋率）
   - 渲染統計卡片和結果表格
   - 啟用下載按鈕

5. **互動功能**
   - 篩選器：可選擇顯示全部/找到的/未找到的單字
   - 下載 TSV：點擊按鈕下載完整分析結果

### NLP 版本工作流程 (`index_nlp.html`)

1. **頁面載入**
   - 從 CDN 載入 Compromise.js 庫
   - 自動載入 `senior6000words.tsv` 資料
   - 初始化 UI 元件

2. **使用者輸入**
   - 使用者在 textarea 中貼上英文文章
   - 點擊「🔍 開始分析」按鈕

3. **文章處理**
   - 使用 Compromise.js 解析文章：`nlp(article)`
   - 提取所有 terms（單字）
   - 清理單字（移除標點符號）
   - 去除重複單字
   - 對每個單字執行 `getLemmaWithNLP()` 進行詞形還原
   - 在 `vocabularyData` 中查找匹配

4. **結果顯示**
   - 計算統計資料
   - 渲染統計卡片和結果表格
   - 顯示 QA 區塊（可編輯原型）
   - 顯示下載區塊（選擇性下載）

5. **人工 QA 階段**
   - 使用者可以編輯黃色背景的「單字原型」欄位
   - 點擊「🔄 重新檢索 6000 單字」按鈕
   - 系統使用更新後的原型重新查找匹配
   - 顯示新找到的單字數量

6. **選擇性下載**
   - 勾選要下載的單字（checkbox）
   - 使用快速選擇功能：全選、取消全選、按等級切換、切換找到/未找到的
   - 即時顯示選擇統計（總計、各等級數量）
   - 點擊「⬇️ 下載選擇的單字為 TSV」
   - 下載的 TSV 包含完整欄位（與 senior6000words.tsv 格式一致）

### 命令列測試工作流程 (`test.js`)

1. **載入測試文章**
   - 讀取 `test_para.txt` 檔案

2. **載入資料**
   - 讀取 `senior6000words.tsv` 檔案
   - 解析並建立詞彙資料結構

3. **分析文章**
   - 使用與輕量版本相同的 `getLemma()` 函數
   - 提取單字並進行詞形還原
   - 查找匹配

4. **顯示結果**
   - 在終端機顯示詳細統計
   - 顯示完整結果表格
   - 列出未找到的單字

5. **生成報告**
   - 生成 TSV 檔案（檔名包含時間戳記）
   - 檔案格式：`測試結果_YYYY-MM-DDTHH-mm-ss.tsv`

## 🔧 技術細節

### 資料格式 (senior6000words.tsv)

TSV 檔案包含以下欄位（Tab 分隔）：
- `Label`: 標籤（例如：`senior6000-L1P01`）
- `英文`: 英文單字（可能包含多種格式）
- `音標`: 音標標記
- `中譯`: 中文翻譯
- `例句`: 英文例句
- `翻譯`: 例句中文翻譯
- `相關影片`: 相關影片連結
- `mark_start`: 標記起始位置
- `mark_end`: 標記結束位置
- `syllables`: 音節劃分
- `main_cefr_label`: CEFR 等級（A1, A2, B1, B2, C1, C2）

### 詞形還原規則詳解

#### 不規則動詞處理
- 預先定義不規則動詞對照表
- 涵蓋 40+ 常用動詞的所有變化形式

#### 規則變化處理順序
1. 不規則動詞檢查
2. 形容詞比較級/最高級（-ier, -iest, -er, -est）
3. 副詞（-ly）
4. 形容詞後綴（-ful）
5. 動詞過去式（-ied, -ed）
6. 動詞進行式（-ing）
7. 名詞複數/動詞第三人稱單數（-s, -es, -ies）

#### 特殊情況處理
- 雙寫子音 + ed/ing（例如：stopped → stop, running → run）
- -ies 結尾（例如：opportunities → opportunity）
- -es 結尾的複雜規則（例如：challenges → challenge, watches → watch）
- -ying 結尾（例如：lying → lie）

### UI/UX 設計特點

- **漸層背景**: 紫色漸層（#667eea 到 #764ba2）
- **響應式設計**: 使用 CSS Grid 和 Flexbox
- **視覺回饋**: 
  - 找到的單字：綠色（#4CAF50）
  - 未找到的單字：紅色（#ff6b6b）
  - 可編輯欄位：黃色背景（#fff9e6）
- **載入動畫**: 旋轉的 spinner
- **篩選功能**: 下拉選單
- **表格設計**: 固定表頭，懸停效果

## 🧪 測試

### 測試檔案
- `test_para.txt`: 包含測試用的英文文章段落

### 執行測試
```bash
node test.js
```

### 測試結果
- 終端機顯示詳細分析報告
- 自動生成 TSV 檔案（檔名包含時間戳記）
- 預期測試結果：
  - 總單字數: 84
  - 找到的單字: 76-77
  - 涵蓋率: 90.5-91.7%

### 常見未找到的單字類型
1. 專有名詞（如：Taiwan）
2. 代名詞（如：it, they, them, their）
3. 不在 6000 單字範圍內的單字
4. 詞形還原失敗的邊界案例

## 🚀 部署

### GitHub Pages 部署

- **⭐ NLP 版本 (推薦預設)**: https://ys-fang.github.io/paragraph-6000words-finder/index_nlp.html
- **輕量版本**: https://ys-fang.github.io/paragraph-6000words-finder/index.html

### 預設入口檔案

**推薦使用**: `index_nlp.html` 作為主要開發和使用的版本
- 所有新功能應優先在 NLP 版本中實現
- NLP 版本包含最完整的功能集
- 輕量版本作為備選方案，用於離線或無網路環境

### 更新部署
```bash
cd /Users/ysfang/Documents/MyCode/paragraph-6000words-finder
git add .
git commit -m "更新說明"
git push origin main
```

GitHub Pages 會自動部署更新（通常需要 1-2 分鐘）。

### 本地測試

由於瀏覽器安全限制，建議使用本地伺服器：
- Python 3: `python -m http.server`
- Python 2: `python -m SimpleHTTPServer`
- Node.js: `npx http-server`

## 💻 開發指南

> ⚠️ **重要**: 開發新功能時，請優先參考和修改 `index_nlp.html`，這是推薦的預設版本。

### 修改詞形還原規則

#### ⭐ NLP 版本 (`index_nlp.html`) **[推薦優先]**
修改 `getLemmaWithNLP()` 函數：
- 調整 Compromise.js 的處理邏輯
- 新增後處理規則
- 這是主要的開發目標版本

#### 輕量版本 (`index.html`)
修改 `getLemma()` 函數：
- 新增不規則動詞：在 `irregularVerbs` 物件中添加
- 修改規則變化：調整各種後綴處理邏輯
- 僅在需要保持離線功能時才需要同步更新

### 新增功能

#### 新增篩選條件
1. 在 HTML 中添加篩選選項
2. 在 `applyFilter()` 函數中添加處理邏輯
3. 更新 `renderTable()` 函數

#### 新增統計資訊
1. 在 `displayResults()` 函數中計算新統計
2. 在 HTML 中添加統計卡片
3. 更新統計顯示邏輯

### 資料格式變更

如果 `senior6000words.tsv` 格式變更：
1. 更新 `loadVocabularyData()` 函數中的欄位索引
2. 更新資料結構定義
3. 更新相關顯示邏輯

## 📝 程式碼風格

- **變數命名**: 使用 camelCase
- **函數命名**: 使用動詞開頭（get, load, find, analyze, display 等）
- **註解**: 關鍵邏輯需要中文註解
- **錯誤處理**: 使用 try-catch 和 console.error
- **使用者提示**: 使用 alert() 顯示重要訊息

## 🐛 已知問題與限制

### 輕量版本
- 某些複雜詞形可能無法正確還原（準確率約 91.7%）
- 邊界情況處理有限（如：changing, challenges）

### NLP 版本
- 首次載入需要網路連接（載入 Compromise.js）
- 載入時間較長（需要載入 ~200KB 的 JavaScript 庫）

### 共同限制
- 專有名詞無法識別（不在 6000 單字表中）
- 代名詞不在 6000 單字表中
- 需要本地伺服器運行（CORS 限制）

## 🔮 未來改進方向

1. **提高準確率**
   - 擴充不規則動詞對照表
   - 改進規則變化處理邏輯
   - 整合更多 NLP 功能

2. **效能優化**
   - 資料快取機制
   - 非同步載入優化
   - 大量文章處理優化

3. **功能擴充**
   - 批次文章處理
   - 匯入/匯出功能增強
   - 統計圖表視覺化
   - 單字學習進度追蹤

4. **使用者體驗**
   - 暗色模式
   - 多語言支援
   - 鍵盤快捷鍵
   - 歷史記錄功能

## 📚 相關文件

- `README.md`: 完整的專案說明（英文）
- `使用說明.txt`: 快速使用指南（繁體中文）
- `版本比較.md`: 兩個版本的詳細比較
- `DEPLOYMENT.md`: 部署資訊

## 🎯 關鍵函數索引

> 📌 **注意**: 開發時優先參考 NLP 版本的函數，這是推薦的預設版本。

### ⭐ NLP 版本 (`index_nlp.html`) **[推薦預設]**
- `getLemmaWithNLP(word)`: NLP 詞形還原函數
- `loadVocabularyData()`: 載入 6000 單字資料
- `findInVocabulary(word)`: 在資料表中查找單字
- `analyzeArticle()`: 分析文章主函數
- `processArticle(article)`: 處理文章內容（使用 Compromise.js）
- `displayResults()`: 顯示分析結果
- `renderTable(data)`: 渲染結果表格
- `updateLemma(index, newLemma)`: 更新單字原型
- `recheckWithUpdatedLemmas()`: 重新檢索 6000 單字
- `toggleWordSelection(index)`: 切換單字選擇
- `updateSelectionStats()`: 更新選擇統計
- `downloadTSV()`: 下載選擇的單字 TSV

### 輕量版本 (`index.html`)
- `getLemma(word)`: 詞形還原函數
- `loadVocabularyData()`: 載入 6000 單字資料
- `findInVocabulary(word)`: 在資料表中查找單字
- `analyzeArticle()`: 分析文章主函數
- `processArticle(article)`: 處理文章內容
- `displayResults()`: 顯示分析結果
- `renderTable(data)`: 渲染結果表格
- `applyFilter()`: 應用篩選器
- `downloadTSV()`: 下載 TSV 檔案

### 命令列版本 (`test.js`)
- `loadVocabularyData()`: 載入 6000 單字資料
- `getLemma(word)`: 詞形還原函數（與輕量版本相同）
- `findInVocabulary(word, vocabularyData)`: 在資料表中查找單字
- `analyzeArticle(article, vocabularyData)`: 分析文章
- `displayResults(results)`: 顯示結果
- `generateTSV(results, outputPath)`: 生成 TSV 檔案
- `showNotFoundWords(results)`: 顯示未找到的單字

---

**最後更新**: 2025-10-20
**維護者**: 專案開發團隊
**授權**: 僅供教育用途

